<!-- filepath: /home/chort-sereivathana/Documents/book-event-project/event-booking/app/views/layouts/application.html.erb -->
<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Event Booking" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Google Font: Source Sans Pro -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <!-- AdminLTE -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/admin-lte@3.2/dist/css/adminlte.min.css">
    <!-- Tailwind CSS (for guest pages) -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= Current.user ? 'hold-transition sidebar-mini layout-fixed' : 'hold-transition login-page' %>">
    <% if Current.user %>
      <!-- Admin/Customer Dashboard Layout -->
      <div class="wrapper">
        <%= render "layouts/navbar" %>
        <%= render "layouts/sidebar" %>
        
        <div class="content-wrapper">
          <!-- Content Header -->
          <div class="content-header">
            <div class="container-fluid">
              <!-- Flash messages -->
              <% if flash[:notice] %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                  <%= flash[:notice] %>
                  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                  </button>
                </div>
              <% end %>
              <% if flash[:alert] %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                  <%= flash[:alert] %>
                  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                  </button>
                </div>
              <% end %>
            </div>
          </div>
          
          <!-- Main content -->
          <section class="content">
            <div class="container-fluid">
              <%= yield %>
            </div>
          </section>
        </div>
        
        <%= render "layouts/footer" %>
      </div>
    <% else %>
      <!-- Guest Layout (Login, Signup, Password Reset) -->
      <div class="min-h-screen bg-gradient-to-br from-blue-500 to-purple-600 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
        <div class="sm:mx-auto sm:w-full sm:max-w-md">
          <div class="text-center">
            <i class="fas fa-calendar-alt text-white text-6xl mb-4"></i>
            <h2 class="text-center text-4xl font-extrabold text-white mb-2">
              Event Booking System
            </h2>
            <%# <p class="text-blue-100 text-sm">Manage your events and bookings</p> %>
          </div>
        </div>
        
        <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
          <div class="bg-white py-8 px-6 shadow-2xl rounded-lg sm:px-10">
            <!-- Flash messages for guest pages -->
            <% if flash[:notice] %>
              <div class="bg-green-50 border-l-4 border-green-400 text-green-700 px-4 py-3 rounded mb-4">
                <div class="flex">
                  <i class="fas fa-check-circle mt-0.5 mr-3"></i>
                  <span><%= flash[:notice] %></span>
                </div>
              </div>
            <% end %>
            <% if flash[:alert] %>
              <div class="bg-red-50 border-l-4 border-red-400 text-red-700 px-4 py-3 rounded mb-4">
                <div class="flex">
                  <i class="fas fa-exclamation-circle mt-0.5 mr-3"></i>
                  <span><%= flash[:alert] %></span>
                </div>
              </div>
            <% end %>
            
            <%= yield %>
          </div>
          
          <p class="mt-6 text-center text-sm text-white">
            &copy; <%= Date.current.year %> Event Booking System. All rights reserved.
          </p>
        </div>
      </div>
    <% end %>

    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Bootstrap 4 -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- AdminLTE App -->
    <script src="https://cdn.jsdelivr.net/npm/admin-lte@3.2/dist/js/adminlte.min.js"></script>
  </body>
</html>